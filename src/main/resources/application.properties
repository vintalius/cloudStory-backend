spring.application.name=cloudstory-backend

# MySQL Connection
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:72.60.127.22}:${MYSQL_PORT:3306}/${MYSQL_DB:cosmic}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:StrongPassword123}

# Driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Game Server Configuration
game.expRate=1x
game.dropRate=2x
game.mesoRate=1x

# JPA Settings
# 'update' keeps your existing MapleStory tables safe while allowing new tables.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Swagger Settings
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# Swagger Admin User
spring.security.user.name=adminTal
spring.security.user.password=Tal!123

# Email Configuration (Resend SMTP)
spring.mail.host=smtp.resend.com
spring.mail.port=587
spring.mail.username=resend
spring.mail.password=re_gcYt4cdT_PpgNsLZesGuVBMsraURz3n3q
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.starttls.protocols=TLSv1.2

# Email settings
app.mail.from=noreply@cloudstory.online
app.mail.from-name=CloudStory

# Frontend URL for password reset links (configurable per environment)
app.frontend.url=https://cloudstory.online

# Voting System Secret Keys
# Set these to the secret keys provided by each voting site
vote.secret.gtop100=CloudStorySecretKey2026
vote.secret.topg=NOT_REQUIRED
vote.secret.xtremetop100=NOT_REQUIRED
vote.secret.arena-top100=bf97ff20e200b9d735d9c5fd